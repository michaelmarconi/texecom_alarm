# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
.coverage
htmlcov/
.pytest_cache/
.ruff_cache/

# Node (tooling-only backlog pin)
node_modules/

# OS
.DS_Store

# agentdef / Guv'nor (regenerated by `agentdef sync`)
.agentdef/
.cursor/

# nested pipeline worktrees (spike/build/plan/refine)
.worktrees/

# Local-sim shims: whole-repo still mounted at apps/local/texecom_alarm until
# Cursor Rebuild remounts texecom_alarm/ (docs/run.md dual-bind). Symlinks only.
/config.yaml
/Dockerfile
/build.yaml
/icon.png
/logo.png
/rootfs
/translations

# raw packet captures - may contain LAN topology/hardware details; findings
# are written up in docs/protocol-reference.md and the relevant spike instead
docs/captures/

# local acceptance / soak evidence (logs, timing extracts) — not for the repo
docs/evidence/

# local session handovers — not for the repo
handovers/
