# Python
__pycache__/
*.pyc
.venv/

# OS
.DS_Store

# agentdef / Guv'nor (regenerated by `agentdef sync`)
.agentdef/
.cursor/

# nested pipeline worktrees (spike/build/plan/refine)
.worktrees/

# raw packet captures - may contain LAN topology/hardware details; findings
# are written up in docs/protocol-reference.md and the relevant spike instead
captures/
